apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'

android {
    namespace "com.akylas.enforcedoze"
    def useExternalSigning = project.hasProperty('useExternalSigning')
    compileSdkVersion 36

    buildFeatures {
        buildConfig = true
        aidl = true
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        // Fork identity: installs alongside the upstream com.akylas.enforcedoze rather than
        // replacing it. The Java/resource namespace above deliberately stays unchanged, so class
        // names and the R class are untouched - only the on-device package identity differs.
        applicationId "com.akylas.enforcedoze.fork"
        minSdkVersion 23
        targetSdkVersion 36
        versionCode 86
        versionName "1.10.2"
        signingConfig signingConfigs.debug
    }
    if (useExternalSigning) {
        signingConfigs {
            release {
                storeFile file(System.getenv("RELEASE_KEYSTORE"))
                storePassword System.getenv("RELEASE_KEYSTORE_PASSWORD")
                keyAlias System.getenv("RELEASE_KEYSTORE_ALIAS")
                keyPassword System.getenv("RELEASE_KEY_PASSWORD")
            }
        }
    }

    buildTypes {
        release {
            vcsInfo.include false
            if (useExternalSigning) {
                signingConfig signingConfigs.release
            }
            multiDexEnabled false
            minifyEnabled true
            // Enables resource shrinking, which is performed by the
            // Android Gradle plugin.
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    dependenciesInfo {
        // Disables dependency metadata when building APKs.
        includeInApk = false
        // Disables dependency metadata when building Android App Bundles.
        includeInBundle = false
    }
}

configurations.all {
    exclude group: 'com.android.support', module: 'support-v13'
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation ('com.afollestad.material-dialogs:core:0.9.3.0') {
        transitive = true
    }
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.browser:browser:1.8.0'
//    implementation 'androidx.legacy:legacy-support-v4:1.0.0'
    implementation 'androidx.media2:media2-session:1.3.0'
    implementation 'androidx.preference:preference-ktx:1.2.1'
    implementation 'eu.chainfire:libsuperuser:1.1.0.+'
    implementation 'com.fabiendevos:nanotasks:1.1.0'
    implementation 'com.jakewharton:process-phoenix:2.1.2'
    implementation 'androidx.localbroadcastmanager:localbroadcastmanager:1.1.0'
    
    // Shizuku
    def shizukuVersion = '13.1.5'
    implementation "dev.rikka.shizuku:api:$shizukuVersion"
    implementation "dev.rikka.shizuku:provider:$shizukuVersion"

//    def libsuVersion = '6.0.0'

    // The core module that provides APIs to a shell
//    implementation "com.github.topjohnwu.libsu:core:${libsuVersion}"

    // Optional: APIs for creating root services. Depends on ":core"
//    implementation "com.github.topjohnwu.libsu:service:${libsuVersion}"

    // Optional: Provides remote file system support
//    implementation "com.github.topjohnwu.libsu:nio:${libsuVersion}"
}
